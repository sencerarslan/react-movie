import { Styled } from './index.styles';

export interface HomePageProps {}

const HomePage = () => {
    return (
        <Styled>
            <div>test</div>
        </Styled>
    );
};
export default HomePage;
