import styled from 'styled-components';

export const PageStyled = styled.div`
    display: flex;
    flex-direction: row;
    .custom-datepicker {
        border: none;
    }
    .custom-button {
        height: 48px;
        margin: 0 2px !important;
    }
`;
